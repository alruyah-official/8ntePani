/**
 * Shared TypeScript types barrel file.
 * Export all application-wide types and interfaces from here.
 *
 * Example:
 *   export type { User } from "./user";
 *   export type { ApiResponse } from "./api";
 */
